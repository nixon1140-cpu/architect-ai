POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
POSTGRES_DB=architectai
# JWT署名鍵。`python -c "import secrets; print(secrets.token_hex(32))"` 等で生成した
# ランダムな値に置き換えること（このプレースホルダーのまま使用しないこと）。
JWT_SECRET_KEY=changeme-generate-a-random-secret
